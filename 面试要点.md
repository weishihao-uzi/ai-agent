# 面试要点

## 项目里的两套 RAG

项目里有两套 RAG 能力：

- `RAG 题库`：已经接入 AI 面试主流程并投入使用。
- `RAG 知识库`：问答型能力已经实现，但目前还没有正式接入具体业务场景。

可以把它们理解为：

- `RAG 题库` 解决的是“问什么、怎么评”的问题。
- `RAG 知识库` 解决的是“回答时参考什么资料”的问题。

## RAG 题库怎么讲

`RAG 题库` 是当前真正落地的核心能力，主要服务于 AI 面试流程。

面试时可以这样描述：

我们把标准面试题、参考答案和采分点整理进题库，并对题目做向量化。用户发起面试时，系统会结合目标岗位和简历技能做语义检索，先从题库里召回最相关的候选题，再交给 AI 进行筛选、微调和补全。这样生成的题目更贴近候选人的岗位和背景，不是完全随机生成。

评分时，题库里的 `reference_answer` 和 `key_points` 会一起传给 AI，作为评分参考依据，所以 AI 不只是“凭感觉评分”，而是有标准答案和采分点可对照，评分会更稳定。

## RAG 知识库怎么讲

`RAG 知识库` 更偏向资料型能力，适合做知识问答和能力增强。

它目前已经实现了完整的技术链路：

- 支持上传 `PDF`、`Markdown`、`TXT` 等文档
- 自动提取文本
- 对文档切分成多个 chunk
- 对每个 chunk 生成 embedding
- 支持按语义检索最相关的知识片段

但目前它还没有正式接入到具体业务闭环里，所以更准确的说法是：

- 技术能力已经实现
- 业务场景暂未正式启用

## 两者的区别

- `RAG 题库`：偏结构化题目资产，直接用于出题和评分
- `RAG 知识库`：偏非结构化资料资产，适合做知识问答和评分增强

题库中的内容通常已经被整理成“题目 + 参考答案 + 采分点”；知识库中的内容通常是原始资料，比如技术文档、学习笔记、规范说明、项目资料。

所以这两套 RAG 不是重复建设，而是分工不同：

- 题库负责面试主流程
- 知识库负责后续扩展能力

## 流程理解补充

这一部分不是面试回答模板，更适合自己理解项目链路时使用。

### 1. DashScope 和 pgvector 分别负责什么

- `DashScope Embedding API` 负责把文本转成向量，也就是 embedding
- `PostgreSQL + pgvector` 负责把向量存进数据库，并执行相似度检索

所以更准确的说法不是“DashScope 在做向量检索”，而是：

- DashScope 负责向量化
- pgvector 负责向量存储和相似度搜索

### 2. 题库检索的实际流程

题库检索时，系统会先把“目标岗位 + 简历技能”拼成一段查询文本，然后：

1. 调用 `DashScope Embedding API` 把 query 转成向量
2. 在 `question_bank` 表里基于 `pgvector cosine_distance` 做相似度搜索
3. 过滤掉低于阈值的题目
4. 把剩余候选题按相似度排序后返回给后续出题逻辑

### 3. 相似度阈值怎么理解

题库里默认阈值是 `0.7`，知识库默认阈值是 `0.3`。

代码里的计算方式是：

- `distance = cosine_distance(query_vec, embedding)`
- `similarity = 1 - distance`

因此：

- `similarity` 越接近 `1`，表示语义越相似
- `similarity` 越接近 `0`，表示语义越不相似

题库检索时，系统默认会过滤掉 `similarity < 0.7` 的题目，也就是只保留语义相关度比较高的候选题。

### 4. 为什么题库和知识库阈值不同

- `RAG 题库` 的目标是“挑出适合直接拿来出题的题”，所以阈值更高，要求更严格
- `RAG 知识库` 的目标是“尽量召回相关资料片段”，所以阈值更低，召回范围更宽

这也是为什么题库更偏精准召回，知识库更偏资料召回。

## 当前落地状态

可以直接这样总结：

- `RAG 题库`：已经投入使用，服务于题目召回、出题增强和评分参考
- `RAG 知识库`：问答型能力已经做完，包括文档上传、切分、向量化和检索，但暂时还没有正式使用

## 题库 RAG 完整流程

如果需要完整讲一遍题库 RAG，可以直接这样说：

我们项目里有一个题库 RAG。管理员先在后台维护面试题，每道题除了题干以外，还会保存参考答案和采分点。系统会把题干和参考答案拼接后做向量化，存进 PostgreSQL + pgvector，完成题库准备。

面试开始时，用户上传简历并选择目标岗位，后端会解析简历、提取技能信息，然后构造 query 并向量化，再去题库里做相似度检索，召回一批最相关的候选题。这些候选题不仅有题目本身，还会带上参考答案和评分要点。接着 AI 会从候选池中筛选并微调出本场面试要用的 N 道题，保存到当前面试记录里。

在用户回答每一道题时，后端会取出这道题对应的参考答案和评分要点，注入到评分 Prompt 中，再交给 DeepSeek 进行评分和反馈。这样 AI 评分不是凭感觉，而是对照题库里的标准答案和采分点来评估，稳定性更高。

## 二次召回与 AI 兜底

题库 RAG 在出题时不是只检索一次，而是有“先严格、再放宽、最后兜底”的三层策略。

### 1. 第一次召回

第一次召回条件更严格，会带上岗位限制，也就是优先只从目标岗位对应的题里找。

例如：

- 目标岗位是 `python_backend`
- 系统会优先只检索 `python_backend` 相关题目

这样做的目的是先保证题目和岗位尽量精准匹配。

### 2. 第二次召回

当第一次召回出来的候选题数量不够时，就会触发第二次召回。

第二次召回会放宽条件，去掉 `position_tag` 限制，再做一次语义检索，用来扩大候选池，提高命中率。

可以理解为：

- 第一次召回：先按岗位严格找
- 第二次召回：如果不够，再放宽岗位限制继续找

### 3. AI 兜底补全

如果两次召回之后，已经有候选题，但数量仍然小于本场面试需要的题目数，就会触发 AI 兜底补全。

这时系统会：

- 保留已经召回到的题库题
- 再让 AI 补齐剩余题目

也就是说，题库题优先，AI 负责补足数量。

### 4. 纯 AI 兜底

如果两次召回之后，一道题都没有找到，就会直接进入纯 AI 生成分支。

这时系统会根据：

- 候选人简历
- 目标岗位
- 目标难度

直接生成整场面试题，不依赖题库。

### 5. 三种分支总结

- 候选题数量 `>= total_questions`：题库充分，AI 从候选池中选题和微调
- 候选题数量 `> 0 且 < total_questions`：题库不够，AI 兜底补全
- 候选题数量 `= 0`：题库为空，纯 AI 生成

### 6. 为什么要这样设计

这样设计的目的是同时兼顾：

- 精准性：优先保证题目和岗位高度相关
- 召回率：题库覆盖不足时还能放宽条件补充候选题
- 稳定性：即使题库题量不够，也不会导致面试流程中断

一句话记忆：

先严格召回，再放宽召回，最后 AI 兜底。

## 面试时的推荐表述

### 30 秒版本

我们项目里做了两套 RAG。第一套是题库 RAG，已经接入 AI 面试流程，用来做题目语义召回、出题增强和评分参考；第二套是知识库 RAG，技术上已经实现了文档摄入和检索问答能力，但目前还没有在正式业务里启用，主要作为后续知识问答和评分增强的能力储备。

### 1 分钟版本

这个项目里，我把 RAG 拆成了两个方向。第一个方向是题库 RAG，它是已经真正投入使用的。我们把标准面试题、参考答案和采分点做成结构化题库，并且对题目做向量化。面试开始时，系统会结合岗位和候选人简历做语义召回，优先从题库里选最相关的题，再由 AI 做筛选和补充。这样题目更贴合候选人的背景，而且评分时还能参考题库里的标准答案和采分点，稳定性更好。

第二个方向是知识库 RAG。它更偏资料检索能力，已经实现了文档上传、切分、向量化和语义检索这套链路。它未来可以接到知识问答，也可以用于增强 AI 面试评分，比如提前上传 Python 后端相关知识文档，用户提问时系统先从知识库检索相关片段，再让 AI 基于资料回答。不过当前项目里，这部分能力已经实现，但还没有正式接入业务闭环。

## 岗位匹配 Agent

### 1. Agent 的主流程怎么讲

岗位匹配 Agent 不是单纯写一个 Prompt，而是让 Agent 按固定流程去调用工具。

它的主流程可以概括为：

1. 读取解析后的简历
2. 构建候选人画像
3. 匹配岗位模板
4. 获取该岗位的面试重点
5. 输出推荐岗位、推荐理由、差距和下一步建议

如果用户确认岗位，还可以继续调用现有面试流程，直接进入专项模拟面试。

这里还有一个很重要的点：

**岗位匹配 Agent 不是凭空推荐岗位，而是先把候选人简历整理成画像，再拿这个画像去和岗位模板表里的岗位模板做匹配。**

所以它的推荐结果不是随便猜出来的，而是基于系统里维护好的岗位模板库做出来的。

### 2. `AgentExecutor` 这一段代码的作用

`AgentExecutor` 可以理解成 Agent 的执行器或者调度器。

它的作用不是定义业务规则，而是负责真正把整个 Agent 跑起来，包括：

- 驱动 Agent 调用工具
- 控制调用顺序和最大轮数
- 处理格式错误重试
- 返回中间步骤，给前端展示“Agent 调用过程”

所以可以这样理解：

- `create_tool_calling_agent(...)` 更像是在定义 Agent 的能力
- `AgentExecutor(...)` 更像是在负责执行这套能力

### 3. 什么是懒加载

懒加载的意思就是：

- 一开始先不创建对象
- 等真正用到的时候再创建

在这个项目里，`_agent_executor` 一开始是 `None`，只有第一次真的调用岗位匹配 Agent 时，才会创建 `AgentExecutor` 实例；后面再次使用时，就直接复用已经创建好的实例。

这样做的好处是：

- 避免程序启动时做无意义初始化
- 减少重复创建 Agent 的开销
- 方便把 Agent 做成单例复用

### 4. 为什么每个工具要自己开数据库会话

LangChain 的 `@tool` 本质上是普通函数调用，它不会走 FastAPI 的 `Depends(get_db)` 依赖注入机制。

所以在 Agent 工具里，不能直接像普通接口那样写：

- `db: AsyncSession = Depends(get_db)`

而是要在每个工具内部自己创建和管理数据库会话，也就是：

- 工具自己开 session
- 工具自己查库
- 工具执行完后自己关闭 session

一句话记忆：

不是 FastAPI 在直接调用这些工具，而是 Agent 在调用，所以数据库会话要由工具自己管理。

### 5. 面试时可以怎么说

可以直接这样说：

岗位匹配 Agent 这部分，我没有做成一个纯 Prompt，而是做成了工具调用型工作流。系统会先读取解析后的简历，再构建候选人画像，然后和岗位模板库做匹配，最后补充该岗位的面试重点，并输出推荐结果。代码层面我用的是 LangChain 的 `create_tool_calling_agent` 加 `AgentExecutor`。其中 `AgentExecutor` 负责真正调度工具调用流程。我还把它做成了懒加载的单例，避免每次请求都重复初始化 Agent，减少运行开销。

### 6. 两个面试入口怎么理解

项目里其实有两个面试入口：

- 一个是用户上传简历后，直接选择目标岗位开始面试
- 另一个是先经过岗位匹配 Agent，拿到推荐岗位后，再开始专项模拟面试

这两个入口的区别在于：

- 普通入口：岗位由用户自己选
- Agent 入口：岗位先由 Agent 推荐

但它们最终复用的是同一套面试主逻辑，也就是同一套：

- 面试创建
- 题库 RAG 召回
- 逐题问答
- AI 评分
- 面试报告生成

一句话记忆：

两个入口，前置流程不同，但最终走的是同一套面试主链路。

## `ai_service.py` 是干什么的

`ai_service.py` 可以理解成项目里的 **AI 能力中枢**，它统一封装了 DeepSeek 的调用，并把大模型能力落到具体业务里。

它主要负责四类事情：

1. **基础模型调用**
   - `._chat()`
   - `._chat_stream()`
   - `._extract_json()`

   这一层负责和 DeepSeek 交互，并尽量把输出转成结构化结果。

2. **简历相关能力**
   - `parse_resume()`
   - `analyze_resume()`

   这一层负责把简历解析成结构化信息，并分析简历和目标岗位的匹配情况。

3. **面试相关能力**
   - `generate_questions()`
   - `evaluate_answer()`
   - `evaluate_answer_stream()`
   - `generate_report()`

   这一层负责出题、评分、流式反馈和最终报告生成。

4. **题库 RAG 的 AI 编排**
   - `select_and_adapt_questions()`
   - `generate_with_seeds()`

   这一层负责和题库召回逻辑配合，决定是从题库里筛选微调，还是用题库题做种子补全。

一句话总结：

**`ai_service.py` 不是单纯调 AI 接口，而是把 DeepSeek 封装成了简历解析、面试出题、回答评分、报告生成和题库补全这些业务能力。**







## 我在项目中的重点实现

如果从“我的技术贡献”角度总结，这个项目最能体现以下几部分能力：

- 设计并落地 **题库 RAG**，实现题库向量化、召回、二次召回和 AI 兜底补全
- 将 `reference_answer + key_points` 引入评分 Prompt，提升评分稳定性
- 设计并实现 **岗位匹配 Agent**，用工具调用工作流串联简历、岗位模板和面试入口
- 基于 `PostgreSQL + pgvector` 完成向量存储与相似度检索
- 完成前后端联调、Docker 本地部署、容器网络与环境配置修复
- 完成知识库文档上传、切分、向量化与检索能力，为后续问答扩展预留能力

---




## 后续可优化方向

- 进一步优化岗位模板和匹配规则，让岗位推荐更精准
- 将知识库 RAG 正式接入问答或评分增强场景
- 增加测试、监控、CI/CD 等工程化能力
- 支持更丰富的岗位类型和面试场景


Agent的5个工具

这 5 个工具把岗位匹配 Agent 拆成了一个标准流水线：先查已解析简历，再提炼候选人画像，然后和岗位模板匹配，接着获取岗位面试重点，最后启动专项模拟面试。


get_parsed_resume
读取数据库里已经解析完成的简历，拿到 parsed_resume。它本质上是“查数据库”，不是重新解析简历。

build_candidate_profile
基于 parsed_resume 再提炼一层候选人画像，输出经验层级、核心技术栈、次要技术栈、项目方向、强项、短板、岗位提示等。这个工具会调用 DeepSeek 做结构化总结。

match_positions
拿候选人画像去和 position_templates 表里的岗位模板做匹配，算出推荐岗位、匹配分数、已匹配技能和缺失技能，输出 Top N 岗位。

get_position_interview_focus
根据岗位 position_tag 读取岗位模板，返回这个岗位的面试重点、推荐难度、推荐题数和检索关键词。它是给后续出题做参数准备的。

start_mock_interview
根据简历 ID 和岗位标签，直接启动一场专项模拟面试。它会结合岗位模板默认参数，调用面试主流程，生成第一道题和面试记录。
