# AI面试项目问答清单

这份文档基于 `AI-Interview` 项目整理，目的是帮助我在面试中更系统地讲清楚项目设计思路、Agent、RAG 和工程化能力。

---

## 1. 项目整体

### Q1：先用一句话介绍一下你的项目

这是一个面向求职场景的 AI 模拟面试平台。用户上传简历后，系统会先用 AI 做简历解析，再通过岗位匹配 Agent 推荐更适合的岗位方向，随后结合题库 RAG 生成更贴合岗位的面试题，并在面试结束后输出评分报告和改进建议。

### Q2：你的项目主要解决什么问题

主要解决三个问题：

- 简历上传后，用户不知道自己更适合什么岗位方向
- 普通 AI 面试题容易泛化，不够贴合具体岗位
- 面试结束后，如果没有结构化反馈，用户很难知道自己的问题在哪里

所以我把简历解析、岗位匹配、RAG 出题、AI 评分和报告生成串成了一条完整链路。

### Q3：你的项目整体业务流程是什么

项目核心流程是：

1. 用户上传 PDF 简历
2. 后端解析简历文本，并调用 AI 生成结构化简历信息
3. Agent 基于结构化简历构建候选人画像
4. Agent 用画像和岗位模板做匹配，输出推荐岗位
5. 系统根据目标岗位走题库 RAG 召回候选题目
6. AI 对召回题目做筛选和微调，生成正式面试题
7. 用户逐题作答，系统结合参考答案和采分点做评分
8. 最后生成评估报告，包括得分、优缺点和学习建议

### Q4：你的项目有哪些核心模块

可以分成 6 个模块：

- 用户端：上传简历、岗位匹配、开始面试、查看报告
- 简历解析模块：抽取结构化简历信息
- 岗位匹配 Agent：构建候选人画像并推荐岗位
- 题库 / 知识库 RAG：负责题目召回和知识检索
- 面试与评分模块：生成题目、逐题评分、生成报告
- 后台管理模块：管理题库、知识库、岗位模板

---

## 2. 项目亮点

### Q5：你觉得这个项目最大的亮点是什么

我觉得有四个亮点：

- 它不是单点 Demo，而是从简历上传到最终报告生成的完整闭环
- 岗位匹配不是纯 Prompt，而是做成了可解释的 Tool Calling Agent
- 出题不是模型随机生成，而是先通过题库 RAG 召回，再做筛选和微调
- 评分不是纯大模型主观打分，而是结合题库中的参考答案和采分点做评估，稳定性更高

### Q6：为什么你项目里的 Agent 和 RAG 值得讲

因为这两部分最能体现 AI 应用落地能力：

- Agent 体现的是流程编排、工具调用、稳定性和可解释性
- RAG 体现的是检索链路设计、参数调优和结果质量控制

这比单纯接一个大模型 API 更能证明项目能力。

---

## 3. Agent 部分

### Q7：你的 Agent 是做什么的

我的 Agent 主要负责岗位匹配。它不是直接出面试题，而是先分析简历、构建候选人画像，再从岗位模板库里推荐更适合的岗位方向，并给出推荐难度、题数和面试重点。

### Q8：为什么要做岗位匹配 Agent

因为用户上传简历后，不一定清楚自己更适合哪个方向。如果直接开始面试，题目可能和用户能力不匹配。  
所以我先让 Agent 做一层岗位匹配，把“简历信息”转成“适合的岗位方向”，这样后面的出题会更准确。

### Q9：你的 Agent 为什么不用一个大 Prompt 直接输出结果

因为一个大 Prompt 虽然实现快，但问题也很明显：

- 不可控
- 不可解释
- 不容易调试
- 结果稳定性差

所以我把岗位匹配做成了 Tool Calling Agent。模型先调用工具读取简历、构建画像、匹配岗位、获取面试重点，这样每一步都能单独看结果，也更方便后续优化。

### Q10：你的 Agent 用了哪些工具

岗位匹配 Agent 主要用了这几个工具：

- `get_parsed_resume`：读取数据库中已解析好的简历
- `build_candidate_profile`：把简历提炼成候选人画像
- `match_positions`：拿画像和岗位模板做匹配
- `get_position_interview_focus`：返回该岗位建议的面试重点、难度和题数
- `start_mock_interview`：基于推荐岗位直接启动模拟面试

### Q11：候选人画像是什么

候选人画像是 Agent 在岗位匹配前构建的一份结构化结果，主要包括：

- `experience_level`
- `primary_stack`
- `secondary_stack`
- `project_directions`
- `strong_points`
- `weak_points`
- `position_hints`

这一步相当于把原始简历内容转成更适合后续匹配的标准输入。

### Q12：你的匹配度算法是怎么实现的

岗位匹配不是让大模型直接给分，而是先让 AI 提炼画像，再用后端规则算法做打分。

具体来说，系统会拿候选人画像去和 `position_templates` 表中的每个岗位模板逐个匹配。匹配度主要由四部分组成：

- 核心技能匹配 `core_score`
- 加分技能匹配 `nice_score`
- 项目方向匹配 `project_score`
- 岗位提示加分 `hints_bonus`

最终分数公式是：

`score = core_score * 0.6 + nice_score * 0.15 + project_score * 0.1 + hints_bonus`

然后对所有岗位模板按分数排序，取 Top 3 作为推荐岗位。

### Q13：为什么说你的 Agent 不是黑盒

因为我返回了 Agent 的中间步骤，前端可以展示完整调用链路，比如：

1. 读取简历
2. 构建画像
3. 匹配岗位
4. 获取面试重点

这样不仅能看最终结果，还能看出它为什么推荐这个岗位。

### Q14：你在 Agent 这块做了哪些工程化控制

主要做了这几个点：

- `max_iterations=10`：限制最大工具调用轮数，避免死循环
- `handle_parsing_errors=True`：模型输出格式异常时做容错处理
- `return_intermediate_steps=True`：返回中间步骤，方便调试和前端展示
- 懒加载单例 `AgentExecutor`：避免每次请求都重复初始化 Agent

### Q15：如果面试官问你 Agent 怎么调优，你怎么答

我会从三个方面答：

- 流程层：工具拆分是否合理，步骤是否过多，是否存在无效调用
- 稳定性层：是否限制最大调用轮数，是否处理模型输出格式异常
- 效果层：看岗位推荐是否合理、画像是否准确、中间步骤是否有偏差

在我的项目里，重点调优点就是工具设计、候选人画像结构和岗位匹配规则。

---

## 4. RAG 部分

### Q16：你的项目为什么要做 RAG

因为如果完全让模型从零生成面试题，题目很容易泛化，和岗位不够贴近。  
所以我做了题库 RAG，让系统先从已有题库中召回相关候选题，再让 AI 做筛选和微调，这样题目更稳定，也更符合岗位方向。

### Q17：你的项目里有哪些 RAG 场景

主要有两个：

- 题库 RAG：用于面试出题
- 知识库 RAG：用于知识检索和问答支撑

### Q18：你的题库 RAG 流程是什么

题库 RAG 流程是：

1. 管理员在后台录入题库数据
2. 题目内容做向量化，并存入 PostgreSQL + pgvector
3. 开始面试时，系统根据岗位方向、难度和关键词去题库做语义召回
4. 召回候选题后，再做筛选和微调
5. 最终生成本轮正式面试题

### Q19：你的知识库 RAG 流程是什么

知识库流程是：

1. 上传 Markdown 等知识文档
2. 用 `RecursiveCharacterTextSplitter` 做分块
3. 用 `DashScope Embedding` 做向量化
4. 存到 PostgreSQL + pgvector
5. 查询时做语义检索，返回相关 chunk 供后续使用

### Q20：你的 RAG 用了什么技术栈

主要是：

- `DashScope Embedding`
- `PostgreSQL`
- `pgvector`
- `RecursiveCharacterTextSplitter`

### Q21：你的文档切分参数是多少，为什么这么设

我的知识库切分参数是：

- `chunk_size=500`
- `chunk_overlap=50`

这样设置是为了平衡语义完整性和检索精度：

- chunk 太小，单块语义不完整
- chunk 太大，多个主题混在一起，向量表达会变差
- overlap 设为 50，可以保留相邻 chunk 的上下文，避免关键信息被切断

### Q22：你的 RAG 检索参数是多少

知识库默认参数是：

- `top_k=4`
- `min_score=0.3`

题库召回参数是：

- `top_k=20`
- `min_score=0.7`

### Q23：为什么知识库和题库的参数不一样

因为两个场景目标不同：

- 知识库更偏“找相关上下文”，所以 `top_k` 可以小一些，`min_score` 可以适当放宽
- 题库更偏“精确召回高相关题目”，所以 `min_score` 要更高，保证题目贴近岗位

### Q24：RAG 里的 top_k 和 min_score 分别是什么意思

- `top_k`：最多召回多少条候选内容
- `min_score`：召回内容的最低相似度阈值

它们决定了检索结果的范围和质量。  
如果 `top_k` 太大、`min_score` 太低，虽然召回更多，但噪音也会变多。

### Q25：你的项目怎么理解 RAG 的召回率和准确率

我会把它理解成两个层面：

- 召回率：相关内容有没有被召回来
- 准确性：召回的内容是不是足够贴合当前问题或岗位

在项目里，我主要通过调 `chunk_size`、`chunk_overlap`、`top_k`、`min_score`，以及看最终题目是否贴合岗位来间接评估效果。

### Q26：如果面试官问你 RAG 怎么调优，你怎么答

我会从四个层面答：

- 数据层：题库和知识库内容是否足够高质量
- 切分层：`chunk_size` 和 `chunk_overlap` 是否合适
- 检索层：`top_k` 和 `min_score` 是否合理
- 业务层：最终召回内容是否真的帮助了出题和问答

在我的项目里，最核心的调优点就是切分参数和召回参数。

---

## 5. 面试与评分

### Q27：你的面试题是怎么生成的

我的项目不是完全让模型即兴出题，而是先用题库 RAG 召回相关题目，再让 AI 在候选题基础上做筛选和微调，这样题目更贴近岗位，也更稳定。

### Q28：为什么评分不能只让模型自由打分

因为如果完全让模型自由打分，主观性会比较强，不同轮次稳定性也可能不足。  
所以我项目里是结合题库中的参考答案和采分点来做评估，让评分尽量有依据。

### Q29：你的评分逻辑有哪些优点

优点有三个：

- 比完全自由打分更稳定
- 更容易解释为什么给这个分数
- 更适合面试场景，因为能直接指出缺了哪些关键点

---

## 6. 工程化能力

### Q30：你的项目有哪些 AI 应用工程化能力

我会从四块来讲：

- Agent 工程化：工具调用、执行控制、调试过程可视化
- RAG 工程化：文档切分、向量化、召回参数设计
- 业务闭环工程化：简历解析到岗位匹配再到出题评分的完整链路
- 部署工程化：Docker、Nginx、Celery、Alembic、日志和健康检查

### Q31：你项目里有做 token 成本评估吗

严格来说，目前还没有做成完整的 token 成本统计面板，但已经有成本控制意识。没有，但是我有成本控制意识

比如：

- Agent 限制了最大调用轮数
- RAG 通过 `top_k` 和 `min_score` 控制上下文规模
- 题目生成不是完全从零生成，而是先召回再筛选

所以我会说：项目已经有成本控制设计，但还没有做到完整的精细化 token 成本观测。

### Q32：为什么说你的项目具备可解释性

因为项目不是只返回最终结果，而是能展示：

- Agent 调用了哪些工具
- 候选人画像是什么
- 为什么推荐这个岗位
- 哪些技能匹配，哪些技能缺失

这使得系统结果不是黑盒。

---

## 7. 部署与后端

### Q33：你的项目是怎么部署的

项目后端使用 Docker 和 Docker Compose 部署，前端静态资源通过 Nginx 提供访问，API 请求再反向代理到 FastAPI 服务。数据库用 PostgreSQL，缓存和消息队列用 Redis，异步任务用 Celery。

### Q34：为什么要用 Celery

因为简历解析、AI 分析、知识库向量化这些操作相对耗时，如果都放在主请求里，会导致接口阻塞。  
所以我用 Celery 把这类任务异步化，提高用户体验和系统吞吐能力。

### Q35：为什么要用 Alembic

因为项目数据库表结构会不断演进，比如新增知识库、岗位模板、简历、面试表等。  
Alembic 可以把数据库变更版本化，方便本地开发和线上部署时保持一致。

### Q36：你的日志和健康检查是怎么考虑的

日志主要是为了方便排查：

- 接口报错
- 数据库异常
- Agent 调用过程问题
- Celery 任务问题

健康检查主要用来快速确认服务状态，比如 API、数据库和 Redis 是否正常。

---

## 8. 常见追问

### Q37：如果让你继续优化这个项目，你会优先做什么

我会优先做三件事：

1. 增加 Agent 和 RAG 的评估机制
2. 做更细粒度的 token 成本统计
3. 进一步优化匹配和召回效果，比如参数调优和测试集验证

### Q38：你这个项目最大的不足是什么

我会比较诚实地说：

- 目前 Agent 和 RAG 已经可运行，但评估体系还不够完整
- token 成本统计还没有做到很精细
- 监控和告警还可以继续完善

但它已经具备了完整的 AI 应用链路和较好的工程化基础。

### Q39：如果面试官说这个项目是不是只是调 API，你怎么回应

我会说不是。  
因为我做的不只是模型调用，还包括：

- Agent 工具流程设计
- 候选人画像和岗位匹配算法
- 题库 / 知识库 RAG
- 数据库存储和模板管理
- 异步任务和部署上线

所以它更像是一个完整的 AI 应用系统，而不是单次模型调用 Demo。

---

## 9. 最后总结版

### Q40：请你总结一下你这个项目最能体现的能力

我觉得这个项目最能体现三类能力：

- AI 应用设计能力：把简历解析、岗位匹配、RAG 出题、评分报告做成完整闭环
- Agent 和 RAG 工程化能力：不是只会接模型，而是能做流程编排、检索链路和参数调优
- 后端与部署落地能力：用 FastAPI、PostgreSQL、Redis、Celery、Docker、Nginx 把系统真正跑起来

这也是我希望通过这个项目向面试官证明的核心能力。



RAG准确率措施
参考答案和采分点写进提示词，主要提升的是生成和评分稳定性；而我项目里真正提高 RAG 准确率的措施，是结构化题库、岗位标签过滤、相似度阈值（0.7）、召回数量控制、查询关键词增强和兜底召回策略。”

