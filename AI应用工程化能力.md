# AI应用工程化能力

这份文档用于梳理 `AI-Interview` 项目中体现出来的 AI 应用工程化能力，重点不是“调用了哪些模型”，而是“如何把 AI 能力做成可控、可调、可解释、可上线的系统”。

## 1. Agent 工程化

项目里的岗位匹配 Agent 不是一个黑盒式调用，而是一个可拆解、可调试、可解释的工具调用流程。

### 1.1 Tool Calling 方式接入

岗位匹配 Agent 使用 LangChain 的 `tool calling agent` 实现，把复杂任务拆成多个工具，由 Agent 逐步调用完成，而不是让大模型一次性输出最终结果。

### 1.2 有明确的执行边界

Agent 不是无限制执行的，项目中设置了：

- `max_iterations=10`：限制最大工具调用轮数，避免循环调用
- `handle_parsing_errors=True`：处理模型输出格式不规范的问题，减少运行失败

当大模型输出格式不符合 Agent 预期时，不要立刻报错退出，而是让 Agent 自动做一次容错处理或重试。


- `return_intermediate_steps=True`：返回中间步骤，便于前端展示调试过程
除了返回最终结果，还把 Agent 中间执行过程一起返回出来。




这几个配置的作用是让 Agent 更稳定，而不是“能跑就行”。

### 1.3 工具链拆分清晰

岗位匹配 Agent 的执行流程被拆成多个独立工具，例如：

- `get_parsed_resume`：读取数据库中已解析的简历
- `build_candidate_profile`：将简历整理成候选人画像
- `match_positions`：拿画像和岗位模板做匹配
- `get_position_interview_focus`：生成面试重点、推荐难度和题数

这种拆分方式的好处是：

- 每一步职责明确
- 出错时容易定位问题
- 推荐结果更容易解释

### 1.4 可解释性

Agent 最终不仅返回推荐结果，还能展示“它是怎么推荐出来的”，包括：

- 读取了哪份简历
- 构建了什么画像
- 匹配到了哪些岗位模板
- 最终为什么推荐这个岗位

这让系统从“黑盒推荐”变成了“可解释推荐”。

## 2. RAG 工程化

项目中的题库召回和知识库问答都不是简单把文本塞给模型，而是做了完整的检索链路设计。

### 2.1 向量化与检索分离

项目使用了：

- `DashScope Embedding`
- `PostgreSQL`
- `pgvector`

流程是先把题库或知识库内容向量化，再存入向量数据库，最后通过语义检索召回候选内容。

这说明项目不是“模型直接生成答案”，而是先检索、再筛选、再生成。

### 2.2 有可调的召回参数

项目中对 RAG 链路做了参数化设计，例如：

- `chunk_size=500`
- `chunk_overlap=50`

- `top_k=20`
- `min_score=0.7`


如果你问的是 知识库 RAG 这组参数，项目里的值是：

top_k = 4
min_score = 0.3
对应位置在 config.py。

如果你问的是 题库召回 那一组，值是：

top_k = 20
min_score = 0.7

这些参数决定了：

- 文档怎么切
- 检索粒度多大
- 一次召回多少条
- 召回内容的最低相似度要求

这就是典型的 RAG 工程化调优点。

### 2.3 题库召回不是直接生成

面试题生成流程是：

1. 先根据岗位和简历信息检索题库
2. 再按岗位、难度、相似度筛选候选题
3. 最后生成实际面试题

这样做的好处是：

- 题目更贴近岗位
- 避免模型随意发挥
- 提升题目稳定性和一致性

## 3. 业务闭环工程化

这个项目不是单点能力展示，而是完整业务链路闭环。

### 3.1 完整链路

核心链路是：

`简历上传 -> AI 解析 -> 数据入库 -> Agent 构建画像 -> 岗位匹配 -> 面试出题 -> AI 评分 -> 生成报告`

这条链路说明项目已经把多个 AI 能力和后端能力串成了一个可运行流程，而不是单独的模型 Demo。

### 3.2 后台数据可配置

项目后台提供了题库、知识库、岗位模板管理能力，说明系统的数据来源不是写死的，而是可配置、可维护的。

这类设计的价值在于：

- 后续可以不断补充题库和知识库
- 岗位模板可以继续扩展
- Agent 和 RAG 的效果可以随着数据增强而提升

### 3.3 结果可落地

前台用户可以直接体验：

- 上传简历
- 岗位匹配
- 模拟面试
- 查看评分和报告

这说明项目最终落到了用户可见的业务结果上，而不是停留在接口层。

## 4. 部署工程化

项目的工程化能力不仅体现在 AI 链路，也体现在部署方式上。

### 4.1 Docker / Docker Compose

项目使用 Docker 将后端、数据库、Redis、Celery 等服务统一编排，减少了本地环境和服务器环境不一致的问题。

### 4.2 Nginx 反向代理

前端静态资源通过 Nginx 提供访问，后端 API 通过反向代理转发，形成标准的前后端分离部署方式。

### 4.3 环境变量配置

项目通过 `.env` 管理数据库、Redis、API Key、端口等配置，使开发环境和生产环境能够区分开来。

### 4.4 Alembic 数据库迁移

数据库表结构通过 Alembic 管理，便于版本演进和线上部署，避免手工改表带来的不可控风险。

### 4.5 Celery 异步任务

简历解析、AI 分析等耗时操作通过 Celery 异步化处理，避免阻塞主请求，提高接口响应速度。

### 4.6 健康检查和日志

项目配置了健康检查和日志系统，方便：

- 快速确认服务是否正常
- 出错时定位问题
- 追踪 AI 调用、数据库访问和任务执行过程

## 5. 面试表达总结

可以把这个项目的工程化能力概括成一句话：

> 这个项目不是简单地接入大模型，而是把 Agent、RAG、异步任务、数据库迁移、容器化部署和日志监控串成了一套可运行、可调优、可解释的 AI 应用系统。

如果面试官继续追问，可以补充：

- Agent 侧看可控性、可解释性、调试能力
- RAG 侧看召回质量、分块策略、相似度阈值
- 部署侧看 Docker、Nginx、Celery、Alembic、日志和健康检查



## 6. 其它

1.项目设计思路

“我这个项目的设计思路，核心是围绕 AI 面试这个业务闭环来做的。首先用户上传简历后，系统会先做 AI 解析；然后通过 Agent 把简历构建成候选人画像，并结合岗位模板完成岗位匹配；接着通过 RAG 从题库里召回更相关的题目，保证出题贴合岗位；最后再进行 AI 评分和报告生成。除了 AI 核心链路，我还做了 Docker、Celery、数据库迁移、Nginx 和日志等工程化设计，保证系统能稳定部署和运行。”



2.文档切换策略

“在我的项目里，RAG 文档切分参数设置的是 chunk_size=500、chunk_overlap=50。chunk_size 设为 500，主要是考虑到如果切得太小，单个片段语义信息不完整，检索出来的内容可能不够支撑后续生成；如果切得太大，又容易把多个主题混在一个 chunk 里，导致向量表达不够聚焦，检索精度下降。chunk_overlap 设为 50，是为了让相邻文本块保留一部分上下文，避免一句关键内容刚好被切断到两个 chunk 里，影响召回效果。这个参数本质上是在完整语义和检索精度之间做平衡。”